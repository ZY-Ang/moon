{
    "version" : "2017-02-28",
    "operation" : "UpdateItem",
    "key" : {
        "email": $util.dynamodb.toDynamoDBJson($util.defaultIfNullOrEmpty($context.identity.claims.email, "guest@paywithmoon.com"))
    },
    "update" : {
        "expression" : "SET #host = :value",
        "expressionNames" : {
           "#host" : "$ctx.args.host"
        },
        "expressionValues" : {
            ":value" : $util.dynamodb.toDynamoDBJson(true)
        }
    }
}