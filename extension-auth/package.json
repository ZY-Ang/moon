{
  "name": "moon-extension-auth",
  "version": "1.0.0",
  "description": "Authentication service for Moon browser extension",
  "main": "index.js",
  "scripts": {
    "deploy-production": "serverless deploy --stage production && aws s3 sync src/ s3://moon.extension.auth --profile moon-production"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.32.0",
    "serverless-single-page-app-plugin": "^1.0.2"
  }
}
